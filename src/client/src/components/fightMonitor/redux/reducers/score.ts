const initState = {
    name: "",
    coach: "",
    penalty: 0,
    advantage: 0,
    point: 0
}

export default (state = initState, action: any) => {
    switch(action){
        default:
            return state;
    }
}