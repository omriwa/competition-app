const initState = {
    timerOn: 0,
    timeToCount: 0,
    timer: 0,
    second: 0,
    minute: 0
}

export default (state = initState,action: any) => {
    switch(action){
        default:
            return state;
    }
}